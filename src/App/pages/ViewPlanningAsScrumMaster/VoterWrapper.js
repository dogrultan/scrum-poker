import styled from 'styled-components';

const VoterWrapper = styled.div`
  display: flex;
  flex-direction: row;
  align-items: flex-start;
  margin-left: 12px;
  p:first-child {
    margin-right: 12px;
  }
`;

export default VoterWrapper;

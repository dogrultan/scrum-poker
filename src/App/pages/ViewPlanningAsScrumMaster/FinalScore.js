import styled from 'styled-components';
import TextField from '../../components/TextField';

const FinalScore = styled(TextField)`
  display: flex;
  margin-bottom: 12px;
`;

export default FinalScore;
